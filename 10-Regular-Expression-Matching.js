/**
 * @param {string} s
 * @param {string} p
 * @return {boolean}
 */
var isMatch = function(s, p) {
    var pattern = p.split("");
    // console.log(pattern)
    var string = s.split("");
    // console.log(string);
    
};


var s = "aa";
var p = "a";
console.log(isMatch(s, p));